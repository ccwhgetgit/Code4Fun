class Solution(object):
    def isPalindrome(self, s):
        """
        :type s: str
        :rtype: bool
        """
        s = s.lower() 

        s = ''.join(s for s in s if s.isalnum())
        
        if s ==s[::-1]: 
            return True 
        else: 
            return False
        
